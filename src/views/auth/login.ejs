<section class="mb-5 p-6 rounded-2xl bg-backgroundmd shadow-lg">
  <h1 class="text-3xl font-bold text-textlt mb-3">Sign In</h1>
  <p class="text-textlt">
    Log in with one of the demo accounts below to access the application.
  </p>
</section>

<% if (pageError) { %>
  <%- include("partials/error", { message: pageError }) %>
<% } %>

<section class="max-w-3xl p-6 rounded-2xl bg-backgroundmd shadow-lg">
  <div class="mb-4">
    <h2 class="text-xl font-semibold mb-2">Log In</h2>
    <p class="text-textlt">
      This starter uses three in-memory demo users with hashed passwords so you
      can focus on building your project features.
    </p>
  </div>

  <form class="grid gap-2.5" method="post" action="/login">
    <label for="email" class="text-sm font-medium">Email</label>
    <input
      id="email"
      name="email"
      type="email"
      autocomplete="username"
      required
      class="w-full px-3 py-2.5 rounded-xl bg-backgrounddk focus:outline-2 focus:outline-highlightlt"
    />

    <label for="password" class="text-sm font-medium">Password</label>
    <input
      id="password"
      name="password"
      type="password"
      autocomplete="current-password"
      required
      class="w-full px-3 py-2.5 rounded-xl bg-backgrounddk focus:outline-2 focus:outline-highlightlt"
    />

    <button
      type="submit"
      class="mt-1 px-4 py-2.5 rounded-lg bg-highlightlt text-white font-medium cursor-pointer hover:bg-highlightlt/80"
    >
      Log in
    </button>
  </form>

  <div class="mt-5 pt-5 border-t border-textlt/10">
    <h3 class="text-base font-semibold mb-2">Demo Users</h3>
    <ul class="list-disc pl-5 space-y-1 text-sm">
      <li><code class="bg-textlt/10 px-1 rounded">admin@app.test</code> / <code class="bg-textlt/10 px-1 rounded">password123</code> / <code class="bg-textlt/10 px-1 rounded">Admin</code></li>
      <li><code class="bg-textlt/10 px-1 rounded">staff@app.test</code> / <code class="bg-textlt/10 px-1 rounded">password123</code> / <code class="bg-textlt/10 px-1 rounded">Staff</code></li>
      <li><code class="bg-textlt/10 px-1 rounded">user@app.test</code> / <code class="bg-textlt/10 px-1 rounded">password123</code> / <code class="bg-textlt/10 px-1 rounded">User</code></li>
    </ul>
  </div>
</section>
